name: Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    name: Test Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.12', '3.13', '3.14']

    steps:
      - name: Checkout code
        uses: actions/checkout@v7.0.0
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v8.3.0
        with:
          version: "latest"

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6.3.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          if [[ "${{ matrix.python-version }}" == "3.14" ]]; then
            echo "Installing dependency groups: dev"
            uv sync --python "${{ matrix.python-version }}" --dev
          else
            echo "Installing dependency groups: dev + litellm-test"
            uv sync --python "${{ matrix.python-version }}" --dev --group litellm-test
          fi

      - name: Check dependency consistency
        run: |
          uv run --python "${{ matrix.python-version }}" --no-sync python --version
          uv run --python "${{ matrix.python-version }}" --no-sync python - <<'PY'
          import importlib.util

          print("litellm installed:", importlib.util.find_spec("litellm") is not None)
          PY
          uv pip check

      - name: Run pytest with coverage
        run: |
          uv run --python "${{ matrix.python-version }}" --no-sync pytest tests/ -n 4 --cov=src/ouroboros --cov-report=xml --cov-report=term-missing -v

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v7
        with:
          name: coverage-${{ matrix.python-version }}
          path: coverage.xml
          if-no-files-found: error
          retention-days: 1

  coverage:
    name: Upload coverage
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: test
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v7.0.0
        with:
          fetch-depth: 0

      - name: Download coverage artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: coverage-*
          path: coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v7.0.0
        with:
          files: ./coverage/coverage-3.12/coverage.xml,./coverage/coverage-3.13/coverage.xml,./coverage/coverage-3.14/coverage.xml
          use_oidc: true
          fail_ci_if_error: false
          flags: unittests
          name: codecov
